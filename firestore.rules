rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGRA DE EMERGÊNCIA: Libera tudo para quem está logado.
    // Isso elimina o erro "Permission Denied" imediatamente.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}